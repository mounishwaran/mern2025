import React,{useEffect, useState} from 'react'

export const Effect = () => {
    const [count, setCount] = useState(0)
    useEffect(()=>)
    return (
        <div>
            <ol>
                {user.map((data)=>

                )}
            </ol>
        </div>
    )
}
export default Effect

